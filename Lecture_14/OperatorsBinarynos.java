package Lecture_14;

import java.util.*;
//Lecture 14 operators and binary numbers 

public class OperatorsBinarynos {
	public static void main(String args[]) {
		// using normal string or stringbuilder, character at particular index can be
		// fetched
		int a = 10;
		int b = 5;
		// one can use: arithmetic operators, relational or logical operators or
		// bitwise, assignment operators.
		// artimetic operators = +, -, *, /, %
		// relational = <<, >>, <=, >=, ==, !=
		// logical operators &&, ||, !
		// number system = decimal, octal, binaryand hexadecimal
		// bitwise operators = &, |, ~, ^, <<, >>
		// assignment operators = =, +=, -=, *=, /=
		System.out.println(a + b);
		// similar way other operators and number systems can be declared
	}
}
